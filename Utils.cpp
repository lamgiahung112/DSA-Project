#pragma once

#include <bits/stdc++.h>

namespace Utils
{
    struct Node {
        std::string name;
        std::vector<Node*> neighbors;
    };

    void bfs(Utils::Node* target, Utils::Node* start, std::vector<std::string>& path) {
        
    }
};